{{>header}}

export type ApiQuery<T, R> = {
	readonly path: string;
    readonly method: string;
    readonly name: string;
    readonly inputs: string[];
    readonly paramTranslator: { [key: string]: string }
    params?: R;
};